# --- Configuration PostgreSQL ---
# Hôte du serveur PostgreSQL (donné par l’app PostgreSQL dans le SSP Cloud > README)
POSTGRES_HOST=

# Port PostgreSQL (généralement 5432 — garde 5432 sauf indication contraire)
POSTGRES_PORT=

# Nom de la base (souvent "defaultdb" par défaut, ou un nom indiqué dans le README)
POSTGRES_DATABASE=

# Identifiants fournis dans le README PostgreSQL (SSP Cloud)
POSTGRES_USER=
POSTGRES_PASSWORD=

# --- Configuration JWT ---
# Clé secrète utilisée pour signer les tokens JWT.
# MPORTANT : cette clé doit être une chaîne de caractères LONGUE, aléatoire et privée.
# Exemple acceptable pour notre projet :
#   SECRET_KEY=super_secret_key_ensai_2025_123456789
#
# Exemple recommandé (généré automatiquement) :
#   SECRET_KEY=b8f3a12bc63b221e75cec98aeff3a6dbe5f34bb1f62e4a94f5c97dea1120a569
#
# Pour générer une clé forte sous Linux/macOS :
#   openssl rand -hex 32
#
# Sous Python :
#   python3 -c "import secrets; print(secrets.token_hex(32))"
#
SECRET_KEY=